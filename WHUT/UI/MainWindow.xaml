<Window x:Class="WHUT.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WHUT.UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="4*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Grid.Row="1" Margin="10, 10, 10, 10">
            <Button Name="NewTournament_Button" Content="New Tournament" Click="NewTournament_Button_Click" />
            <Button Name="CreatePlayer_Butoon" Content="Create Player" Click="CreatePlayer_Butoon_Click"/>
        </StackPanel>

        <Frame Name="ContentFrame" Grid.Column="1" Grid.Row="1"
               Margin="10, 10, 10, 10" 
               NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
